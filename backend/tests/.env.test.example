# Test Environment Configuration
# Copy this file to .env.test and configure your test environment
# NEVER commit .env.test to version control

# Database Configuration for Tests
DB_HOST=localhost
DB_NAME=hypnose_stammtisch_test
DB_USER=test_user
DB_PASS=secure_test_password

# Security Test Configuration
MAX_FAILED_ATTEMPTS=3
TIME_WINDOW_SECONDS=300
IP_BAN_DURATION_SECONDS=600
ACCOUNT_LOCK_DURATION_SECONDS=600
HEAD_ADMIN_ROLE_NAME=head

# Test User Credentials (Optional - if not set, random passwords will be generated)
# These should be secure and different from production passwords
TEST_USER_PASSWORD=SecureTestPassword123!
TEST_ADMIN_PASSWORD=SecureAdminTestPassword456!

# Security Note:
# If TEST_USER_PASSWORD and TEST_ADMIN_PASSWORD are not set,
# the tests will automatically generate secure random passwords.
# This is the recommended approach for automated testing environments.
